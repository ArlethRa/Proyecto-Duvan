create table usuarios (
cedula int primary key not null, 
nombre varchar(20),
apellido varchar(20),
usuario varchar(20)
);

CREATE TABLE `proyectoapi`.`productos` (
  `id_productos` INT NOT NULL AUTO_INCREMENT,
  `nom_productos` VARCHAR(45) NULL,
  `cantidad` INT NULL,
  `productoscol` VARCHAR(45) NULL,
  PRIMARY KEY (`id_productos`));

ALTER TABLE `proyectoapi`.`usuarios` 
ADD COLUMN `telefono` VARCHAR(45) NULL AFTER `usuario`;

